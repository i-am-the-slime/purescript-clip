package:
  name: clip-example-basic
  dependencies:
    - clip
    - effect
    - prelude
    - console
  build:
    strict: true

workspace:
  package_set:
    registry: 49.0.0
  extra_packages:
    clip:
      git: https://github.com/i-am-the-slime/purescript-clip.git
      ref: v0.1.0
  backend:
    cmd: echo "This example uses purescript-native Go backend"
    dependencies: []